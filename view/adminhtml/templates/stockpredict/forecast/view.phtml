<?php
/**
 * Forecast Detail View Template
 *
 * Magento 2 Stock prediction using AI
 * @author Hassan Ali Shahzad
 * email: levosoft786@gmail.com
 *
 * @var \AI\StockPredict\Block\Adminhtml\Forecast\View $block
 */
declare(strict_types=1);

$forecast = $block->getForecast();

if (!$forecast): ?>
<div class="message message-error error">
    <div><?= $block->escapeHtml(__('Forecast not found.')) ?></div>
</div>
<?php return; endif;

$severity      = (string)$forecast->getData('severity');
$severityColor = match ($severity) {
    'critical' => '#e22626',
    'warning'  => '#e07f00',
    'watch'    => '#d4b800',
    default    => '#333',
};
$trend         = (string)($forecast->getData('trend') ?? 'stable');
$trendIcon     = match ($trend) {
    'rising'   => '&#8599;',
    'declining' => '&#8600;',
    default    => '&#8594;',
};
?>
<div class="stockpredict-forecast-detail" style="max-width:900px;">

    <!-- Back button -->
    <div style="margin-bottom:20px;">
        <a href="<?= $block->escapeUrl($block->getBackUrl()) ?>" class="action-default scalable back">
            &larr; <?= $block->escapeHtml(__('Back to Forecasts')) ?>
        </a>
    </div>

    <h2 style="margin-bottom:4px;">
        <?= $block->escapeHtml(__('Forecast Detail')) ?>
        <span style="color:<?= $block->escapeHtmlAttr($severityColor) ?>;font-size:0.75em;margin-left:10px;">
            [<?= $block->escapeHtml(strtoupper($severity)) ?>]
        </span>
    </h2>
    <p style="color:#777;margin-top:4px;">
        <?= $block->escapeHtml((string)$forecast->getData('sku')) ?>
        <?php if ($forecast->getData('product_name')): ?>
            — <?= $block->escapeHtml((string)$forecast->getData('product_name')) ?>
        <?php endif; ?>
    </p>

    <!-- Main data sections -->
    <div style="display:flex;gap:20px;flex-wrap:wrap;margin-top:20px;">

        <!-- Stock & Stockout -->
        <div style="flex:1;min-width:260px;background:#fff;border:1px solid #ddd;border-radius:4px;padding:20px;">
            <h3 style="margin-top:0;border-bottom:1px solid #eee;padding-bottom:8px;"><?= $block->escapeHtml(__('Stock & Stockout')) ?></h3>
            <table style="width:100%;border-collapse:collapse;">
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;width:55%;"><?= $block->escapeHtml(__('Current Stock Qty')) ?></th>
                    <td style="padding:7px 0;font-weight:bold;"><?= $block->escapeHtml(number_format((float)$forecast->getData('current_qty'), 2)) ?></td>
                </tr>
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;"><?= $block->escapeHtml(__('Avg Daily Sales')) ?></th>
                    <td style="padding:7px 0;"><?= $block->escapeHtml(number_format((float)$forecast->getData('avg_daily_sales'), 4)) ?></td>
                </tr>
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;"><?= $block->escapeHtml(__('Days Until Stockout')) ?></th>
                    <td style="padding:7px 0;font-weight:bold;color:<?= $block->escapeHtmlAttr($severityColor) ?>;">
                        <?= $forecast->getData('days_until_stockout') !== null ? (int)$forecast->getData('days_until_stockout') : $block->escapeHtml(__('N/A')) ?>
                    </td>
                </tr>
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;"><?= $block->escapeHtml(__('Predicted Stockout Date')) ?></th>
                    <td style="padding:7px 0;"><?= $forecast->getData('predicted_stockout_date') ? $block->escapeHtml((string)$forecast->getData('predicted_stockout_date')) : '—' ?></td>
                </tr>
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;"><?= $block->escapeHtml(__('Severity')) ?></th>
                    <td style="padding:7px 0;font-weight:bold;color:<?= $block->escapeHtmlAttr($severityColor) ?>;"><?= $block->escapeHtml(strtoupper($severity)) ?></td>
                </tr>
            </table>
        </div>

        <!-- Demand Forecast -->
        <div style="flex:1;min-width:260px;background:#fff;border:1px solid #ddd;border-radius:4px;padding:20px;">
            <h3 style="margin-top:0;border-bottom:1px solid #eee;padding-bottom:8px;"><?= $block->escapeHtml(__('Demand Forecast')) ?></h3>
            <table style="width:100%;border-collapse:collapse;">
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;width:55%;"><?= $block->escapeHtml(__('Forecast 7d Demand')) ?></th>
                    <td style="padding:7px 0;"><?= $block->escapeHtml(number_format((float)$forecast->getData('forecast_demand_7d'), 2)) ?></td>
                </tr>
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;"><?= $block->escapeHtml(__('Forecast 30d Demand')) ?></th>
                    <td style="padding:7px 0;font-weight:bold;"><?= $block->escapeHtml(number_format((float)$forecast->getData('forecast_demand_30d'), 2)) ?></td>
                </tr>
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;"><?= $block->escapeHtml(__('Forecast 90d Demand')) ?></th>
                    <td style="padding:7px 0;"><?= $block->escapeHtml(number_format((float)$forecast->getData('forecast_demand_90d'), 2)) ?></td>
                </tr>
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;"><?= $block->escapeHtml(__('Trend')) ?></th>
                    <td style="padding:7px 0;"><?= $trendIcon ?> <?= $block->escapeHtml(ucfirst($trend)) ?></td>
                </tr>
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;"><?= $block->escapeHtml(__('Confidence Score')) ?></th>
                    <td style="padding:7px 0;"><?= $block->escapeHtml(number_format((float)$forecast->getData('confidence_score') * 100, 0)) ?>%</td>
                </tr>
                <tr>
                    <th style="text-align:left;padding:7px 0;color:#666;font-weight:normal;"><?= $block->escapeHtml(__('Recommended Reorder Qty')) ?></th>
                    <td style="padding:7px 0;font-weight:bold;color:#1a7f37;"><?= $forecast->getData('recommended_reorder_qty') !== null ? $block->escapeHtml(number_format((float)$forecast->getData('recommended_reorder_qty'), 0)) : '—' ?></td>
                </tr>
            </table>
        </div>

    </div>

    <!-- Historical Sales -->
    <div style="background:#fff;border:1px solid #ddd;border-radius:4px;padding:20px;margin-top:20px;">
        <h3 style="margin-top:0;border-bottom:1px solid #eee;padding-bottom:8px;"><?= $block->escapeHtml(__('Historical Sales')) ?></h3>
        <div style="display:flex;gap:30px;flex-wrap:wrap;">
            <div style="text-align:center;min-width:120px;">
                <div style="font-size:1.8em;font-weight:bold;color:#333;"><?= (int)$forecast->getData('last_30d_sales') ?></div>
                <div style="color:#777;font-size:0.9em;"><?= $block->escapeHtml(__('Units (last 30d)')) ?></div>
            </div>
            <div style="text-align:center;min-width:120px;">
                <div style="font-size:1.8em;font-weight:bold;color:#333;"><?= (int)$forecast->getData('last_90d_sales') ?></div>
                <div style="color:#777;font-size:0.9em;"><?= $block->escapeHtml(__('Units (last 90d)')) ?></div>
            </div>
            <div style="text-align:center;min-width:120px;">
                <div style="font-size:1.8em;font-weight:bold;color:#333;"><?= (int)$forecast->getData('last_365d_sales') ?></div>
                <div style="color:#777;font-size:0.9em;"><?= $block->escapeHtml(__('Units (last 365d)')) ?></div>
            </div>
        </div>
    </div>

    <!-- AI Recommendation -->
    <?php if ($forecast->getData('ai_recommendation')): ?>
    <div style="background:#f0f7f0;border:1px solid #b2d8b2;border-radius:4px;padding:20px;margin-top:20px;">
        <h3 style="margin-top:0;color:#1a6b1a;"><?= $block->escapeHtml(__('AI Recommendation')) ?></h3>
        <p style="margin:0;color:#2d5a2d;line-height:1.6;"><?= $block->escapeHtml((string)$forecast->getData('ai_recommendation')) ?></p>
    </div>
    <?php else: ?>
    <div style="background:#f9f9f9;border:1px solid #e0e0e0;border-radius:4px;padding:16px;margin-top:20px;color:#999;">
        <?= $block->escapeHtml(__('No AI recommendation available. Enable AI analysis in Stores → Configuration → AI → Stock Predict.')) ?>
    </div>
    <?php endif; ?>

    <!-- Metadata -->
    <div style="margin-top:20px;padding:12px 16px;background:#f9f9f9;border:1px solid #e0e0e0;border-radius:4px;font-size:0.85em;color:#777;">
        <?= $block->escapeHtml(__('Forecast ID: #%1', $forecast->getId())) ?>
        &nbsp;|&nbsp;
        <?= $block->escapeHtml(__('Created: %1', (string)$forecast->getData('created_at'))) ?>
        &nbsp;|&nbsp;
        <?= $block->escapeHtml(__('Last Updated: %1', (string)$forecast->getData('updated_at'))) ?>
    </div>

</div>
